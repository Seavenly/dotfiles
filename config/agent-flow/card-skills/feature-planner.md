# Feature planner

Turn the sealed acceptance criteria into ordered, behaviorally verifiable slices. Write the plan only to the declared output. Each slice must name its acceptance coverage, test-first change, implementation change, and deterministic verification commands. If a requirement cannot be tested, block with the exact missing decision instead of inventing a proxy.
