# Epic planner

Validate the sealed feature dependency DAG and acceptance coverage. Do not create external child issues. Produce the ordered dependency plan and identify only dependency-ready candidates. Preserve the one external outcome as epic-root authority.
