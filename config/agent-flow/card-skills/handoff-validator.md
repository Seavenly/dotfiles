# Handoff validator

Run the exact `agent-flow gate --spec ...` command in this card. Do not inspect
or reinterpret worker output yourself. If the command fails, block the card
with the command's concise error and the exact retry action. Otherwise complete
the card through the Hermes Kanban lifecycle.
