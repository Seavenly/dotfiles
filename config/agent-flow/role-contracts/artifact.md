# Artifact role contract

- Treat the candidate repository and pinned revisions as read-only.
- Produce only the artifact declared by the card.
- Use inline completion metadata when the card declares an inline output.
- Keep evidence traceable to the pinned change and end with a Kanban lifecycle
  call.
