interface:
  display_name: "Agent Flow Stacks"
  short_description: "Plan and build immutable review stacks safely"
  default_prompt: "Use $agent-flow-stacks to plan or operate an approved immutable review stack."
