schema = "drovr.capability/v1"

[codex]
sandbox = "read-only"
approval = "never"
search = false

[claude]
permission_mode = "dontAsk"
allowed_tools = ["Read", "Glob", "Grep", "Bash(git diff *)", "Bash(git status *)", "Bash(git log *)", "Bash(git show *)", "Bash(git rev-parse *)"]
