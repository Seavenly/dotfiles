{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dotfiles.local/schemas/flow.transport-error.v1.schema.json",
  "title": "flow.transport-error/v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "version", "code"],
  "properties": {
    "schema": { "const": "flow.transport-error/v1" },
    "version": { "const": 1 },
    "code": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]{0,63}$"
    }
  }
}
