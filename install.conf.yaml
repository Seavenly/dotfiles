- clean: ['~']

- link:
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.hammerspoon: hammerspoon
    ~/.tmux.conf: tmux.conf

- shell:
    - [git submodule update --init --recursive, Installing submodules]
